({
	runInit: function (cmp, event, helper) {
  		helper.helpgetItemsToApprove(cmp, event);
    },
    openLog: function (cmp, event, helper) {
  		helper.helpopenLog(cmp, event);
    },
    refreshTable: function (cmp, event, helper) {
  		helper.helpgetItemsToApprove(cmp, event);
    }
})